# TODOs

Poor man's issue management for TabuLua.

## Index

| File | Summary |
| --- | --- |
| [lua55_compatibility.md](lua55_compatibility.md) | Lua 5.5 compatibility issues with the ltcn library |
| [luajit_compatibility.md](luajit_compatibility.md) | LuaJIT compatibility issues and proposed `numbers.lua` abstraction layer |
| [pre_processors.md](pre_processors.md) | Pre-processors (landed) — sandboxed expressions that mutate parsed rows after `=expr` evaluation but before validators. `priority` ordering, `rerunAfterPatches` for cross-package mod patches, `processor_spec` type alias, dedicated `processor_executor` module. |
| [graph_types.md](graph_types.md) | Plan for built-in `basic_graph_node` / `graph_node` / `tree_node` types with auto-wired completion and validators, plus optional edge files |
| [mod_overrides.md](mod_overrides.md) | Research and plan for child-package modifications to parent data (mod-style overrides: add/remove/update rows, filter-and-transform, validation re-run) |
| [cog_markdown.md](cog_markdown.md) | Plan for running COG on `.md` (and other non-TSV text) files, to **generate documentation from TSV data**. Part 1: a hidden HTML-comment marker style — sigil `<!---` … `--->` (distinct from plain `<!-- -->`), invisible in rendered/raw Markdown, with a recommended block form for multi-line code. Part 2: triggering via **auto-scan by eligible extension gated by `needsCog`** (recommended over an explicit per-file list), with two modes (generate-to-export and in-place refresh). Resolves content_pipeline.md Q8/Q9. |
| [content_pipeline.md](content_pipeline.md) | Plan for a second registry (sibling of type-wiring): file-name / extension / directory-keyed **content stages** that transform a file's raw bytes around TSV parsing. **Bidirectional** — a *source* direction on read (`decode` decompress → `transcode` XML/JSON/SQLite/`.mtx`→TSV → `macro` COG-expand) and a *sink* direction on export (inverse), with COG present in both (expand on read, **strip COG comments on export**). COG is **format-agnostic** — works on any file using its `---`/`###`/`///` comment styles, not just TSV — enabling **data-driven generation of non-data text files** (e.g. Markdown docs from TSV data) at export time, with an open question on a hidden HTML-comment marker style for clean Markdown source. Tracks a per-file **content kind** (`text`/`binary`) so it also handles **binary/asset files** (images, audio, fonts): the load decision is **"does a stage need the bytes?"** not known-vs-unknown, so any binary nothing processes — recognised type or not — is **never loaded into memory** (`raw_files` holds an O(1) passthrough descriptor and the exporter **block-streams** the original via `copyFileStreamed`), while a binary a matched `asset`-phase stage (extension/magic-keyed) claims is loaded to re-encode/resize; text transforms (EOL/COG/parse) are walled off from binary. Extension peeling for chained decoders, binary reads + explicit EOL normalisation, decompression/image-bomb caps, reformatter round-trip via `reversible`/`sinkTransform`. Collapses the three hard-coded `processContentBV` call sites into one; `exporter.lua` is the sink driver. |
| [type_wiring.md](type_wiring.md) | Refactor (post-graphs): collapse hard-wired super-type behaviour branches (`Type`/`enum`/`custom_type_def`/graph wiring) into a single type-wiring registry. Per-typeName `register` (cascade: `onLoad`, processors, validators) and module-level `registerModule` (`enginePostPasses`, `sandboxHelpers`, `descriptorColumns`). Shrinks core `Files.tsv` to six intrinsic columns; the other ten become feature-module registrations. User packages reach the registry via a new manifest `bootstrap` field (code library, reaches all slots except `onLoad`) and via the built-in `type_wiring_def` record type (pure-data, expression slots only — files are recognised by record type, not by basename) |
| [read_only_next_bypass.md](read_only_next_bypass.md) | Close the `next()` bypass in `read_only.lua` by moving the proxy→original mapping into a module-private weak table |
| [matrix_market_coo.md](matrix_market_coo.md) | Extend `raw_tsv` with Matrix Market / COO (`.mtx`) reader, writer, validator, and header parser for sparse-matrix coordinate-list files |
| [pk_lookup_audit.md](pk_lookup_audit.md) | Audit the codebase for places that rebuild a name→row map or linear-scan for a PK that the parsed dataset already indexes natively, and fix them via three documented patterns |
| [data_mutation_integrity.md](data_mutation_integrity.md) | Make all data writes go through `setCell` (stop `wrapRowForProcessor` handing out mutable tables), close the cell-expression/COG sandbox escape via `loadEnv`'s `__index = _G`, and centralize the scattered sandbox-environment definitions into a single `sandbox_env.lua` |
